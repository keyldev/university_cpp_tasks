#include <iostream>
using namespace std;
int main(){
int a, b;
   cin >> a;
   cin >> b;
   cout << "Результат сложения = " << a + b << endl << "Результат вычитания = " << a - b << endl << "Результат умножения = " << a * b << endl << "Результат деления = " << a / b << endl;
   double a, b;
   cin >> a;
   cin >> b;
   cout << "Результат сложения = " << a + b << endl << "Результат вычитания = " << a - b << endl << "Результат умножения = " << a * b << endl << "Результат деления = " << a / b << endl;
   int a;
   double b;
   cin >> a;
   cin >> b;
   cout << "Результат сложения = " << a + b << endl << "Результат вычитания = " << a - b << endl << "Результат умножения = " << a * b << endl << "Результат деления = " << a / b << endl;
  
return 0;
}